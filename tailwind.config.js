/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    darkMode: "class", // or 'media' for automatic dark mode based on user settings
    extend: {},
  },
  plugins: [],
}
